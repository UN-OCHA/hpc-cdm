import { flows } from '@unocha/hpc-data';
import { FlowFormType } from '../components/flow-form';

//  TODO: Implement this function
export const parseFlowForm = (values: FlowFormType): flows.CreateFlowParams => {
  return values as any;
};
